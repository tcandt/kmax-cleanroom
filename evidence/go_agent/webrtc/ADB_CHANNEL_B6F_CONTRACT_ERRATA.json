{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "metadata": {
    "phase": "Phase 2C.5B6F",
    "title": "ADB-Channel Formal Contract & Historical Evidence Errata",
    "description": "Formally supersedes preliminary Phase 2C.5A claims regarding adb-channel downstream topology and inbound channel options, recording machine-bound corrections while preserving historical artifact immutability.",
    "superseded_historical_artifacts": [
      {
        "artifact": "evidence/go_agent/webrtc/DATACHANNEL_FRAMING_MATRIX.json",
        "superseded_claim": "adb-channel target downstream: 'Local ADB daemon / 127.0.0.1:5555 / reverse shell'"
      },
      {
        "artifact": "reports/30_PHASE2C5A_WEBRTC_DATACHANNEL_MEDIA_FORENSICS.md",
        "superseded_claim": "adb-channel target / subsystem: 'Reverse shell / local ADB daemon bridge'"
      }
    ],
    "errata_principles": [
      "Historical Phase 2C.5A artifacts remain strictly immutable on disk.",
      "Exhaustive binary callgraph analysis across ARM64 and AMD64 proves zero reachable net.Dial/net.DialTCP calls, zero constructed socket paths, and zero occurrences of '127.0.0.1:5555'.",
      "The authentic original downstream topology is an in-process PTY shell allocation (/dev/ptmx via pty.Open with non-PTY pipe shell fallback) spawning /system/bin/sh (ARM64) or /bin/sh (AMD64) with background goroutines pumping stdout -> (*DataChannel).Send.",
      "Inbound 'ordered: true' property is classified as REFERENCE_ONLY from frontend browser creation (pc.createDataChannel('adb-channel', { ordered: true })), as original Agent binary does not enforce or inspect the ordered flag on inbound channels.",
      "All process execution, PTY allocation, and shell streaming logic are classified as DEFERRED_ADB_BRIDGE_BOUNDARY and excluded from clean-room agent runtime implementation."
    ]
  },
  "corrections": [
    {
      "contract_id": "ADB-B6F-ERRATA-01",
      "superseded_claim": "adb-channel bridges to an external local ADB daemon at 127.0.0.1:5555 over TCP socket",
      "problem": "Phase 2C.5A preliminary documentation assumed an external ADB daemon bridge based on standard Android cloud patterns. However, exhaustive callgraph disassembly of both ARM64 and AMD64 binaries proves zero net.Dial calls reachable from adb-channel, zero TCP socket logic, and zero occurrences of '127.0.0.1:5555' in binary rodata/symbols.",
      "supporting_original_evidence": "Reachable call graph from ARM64 0x516020 and AMD64 0x9b2be0 contains 0 net.Dial calls; strings '127.0.0.1:5555' and '5555' TCP endpoints absent.",
      "corrected_original_claim": "The original Agent embeds an in-process ADB-packet compatible handler and PTY reverse-shell boundary spawning local shell (/system/bin/sh or /bin/sh). No external TCP ADB daemon connection exists.",
      "effective_classification": "STATIC_CONFIRMED",
      "effective_mandatory_for_original_parity": true
    },
    {
      "contract_id": "ADB-B6F-ERRATA-02",
      "superseded_claim": "adb-channel ordered=true is an Agent-side static protocol guarantee",
      "problem": "The ordered=true configuration is specified exclusively on the browser client during pc.createDataChannel('adb-channel', { ordered: true }). The Agent binary accepts the channel via pc.OnDataChannel and never inspects or enforces the (*DataChannel).Ordered() flag.",
      "supporting_original_evidence": "ARM64 OnDataChannel (0x53f210) and AMD64 OnDataChannel (0x9e2f20) call (*DataChannel).Label() only; (*DataChannel).Ordered() is never called.",
      "corrected_original_claim": "Channel ordered property is a browser client creation property (REFERENCE_ONLY) and does not constitute an Agent-side static parity requirement.",
      "effective_classification": "REFERENCE_ONLY",
      "effective_mandatory_for_original_parity": false,
      "mandatory_for_interoperability": true
    }
  ]
}
